{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "2.1.0",
    "natalie-reynolds-capstone-medz": "file:../..",
    "twilio": "^5.12.0"
  },
  "devDependencies": {
    "twilio-run": "^5.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
